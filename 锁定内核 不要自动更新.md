---
created: 2026-08-27T17:36:21+08:00
modified: 2026-08-27T17:36:36+08:00
---

# 锁定内核 不要自动更新

在 Ubuntu 中禁用自动更新，主要有四种方法，从图形界面到命令行都有。综合安全性和灵活性，最推荐的方法是“锁定内核包 + 禁用无人值守升级”。

下面是这几种方法的对比和具体操作：

🖥️ 方法一：通过图形界面（最简单）

此方法适合不熟悉命令行的用户，能关闭系统更新检查。

1. 打开 “软件和更新” (Software & Updates)。
2. 切换到 “更新” (Updates) 标签页。
3. 将 “自动检查更新” 下拉菜单选为 “从不”。

⌨️ 方法二：修改配置文件（更彻底）

手动配置 APT，直接从源头禁止自动更新。该方法对所有软件包（含内核）生效。

```bash
# 1. 编辑配置文件
sudo nano /etc/apt/apt.conf.d/20auto-upgrades

# 2. 将文件内容修改或确保为以下内容（将所有 "1" 改为 "0"）
APT::Periodic::Update-Package-Lists "0";
APT::Periodic::Download-Upgradeable-Packages "0";
APT::Periodic::AutocleanInterval "0";
APT::Periodic::Unattended-Upgrade "0";
```

保存后（Ctrl+O，回车，Ctrl+X），可再编辑 /etc/apt/apt.conf.d/10periodic 做同样修改。

🔒 方法三：锁定特定内核包（最精准，强烈推荐）

只阻止内核更新，不影响其他软件安全更新。

```bash
# 锁定当前内核的镜像和头文件包
sudo apt-mark hold linux-image-$(uname -r) linux-headers-$(uname -r)
```

之后，执行 sudo apt-mark showhold 可查看被锁定的包。如需解锁，将 hold 换为 unhold 即可。

🛑 方法四：禁用无人值守升级服务 (Unattended Upgrades)

直接关闭后台自动安装安全更新的服务。

```bash
# 立即停止并禁用该服务
sudo systemctl disable --now unattended-upgrades
```

---

💎 总结与建议

· 只为保护自定义内核：执行 sudo apt-mark hold 锁定内核包。
· 完全控制系统所有更新：组合使用 sudo systemctl disable --now unattended-upgrades 和修改 20auto-upgrades 配置文件。

⚠️ 重要提醒：禁用自动更新后，系统不会自动安装重要的安全补丁。请务必养成定期手动检查并安装更新的习惯，确保系统安全。
